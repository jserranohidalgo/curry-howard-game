# sbt 2.x is installed as the launcher, but its plugin ecosystem is not one we
# want to bet the schedule on. Pin the current 1.x line here; the launcher
# fetches whatever this file asks for.
sbt.version=1.12.15
